{% load_yaml as rawmap %}
Debian:
  client:
    pkgs:
      - nfs-common
  server:
    pkgs:
      - nfs-common
      - nfs-kernel-server
RedHat:
  client:
    pkgs:
      - nfs-utils
  server:
    pkgs: []
      #- TODO nfs-kernel-server
{% endload %}
